import PageMessage from "./PageMessage";

const Repair = ({ title = "Sahifa ta'mirlanmoqda...", className }) => {
  return <PageMessage title={title} className={className} />;
};

export default Repair;
